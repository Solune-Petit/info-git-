import importlib

# Spécifiez le nom complet du module (y compris le sous-dossier)
module_name = "levels.fingerdash"

# Utilisez la fonction import_module() pour importer le module
fingerdash_module = importlib.import_module(module_name)